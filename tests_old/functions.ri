let greet_factory = fn(name)
    fn()
        print("Hello, " + name + "!")
    end
end

let world_greeter = greet_factory("Wolrd")
world_greeter()

let fn variadic(param, ..rest)
    print(param)
    print("====")
    print(..rest)
end

variadic("some param", 2, 3, 4)
